/*angular.module('app')
.controller('LogoutCtrl', function ($scope, UserSvc){
  /*$scope.logout = function() {
    //UserSvc.logout()
    /*.then(function(user){
      //console.log(user)
      //Use $emit to pass the event up
      $scope.$emit('logout', user.data)
      $location.path('/')
    })
    console.log('logout in process')
    $scope.$emit('logout', user.data)
  }*
})*/
